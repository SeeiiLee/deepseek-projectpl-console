# 统一项目治理文件闭环复核（2026-08-25）

> 历史快照：本文记录 Project Home 创建前的文件闭环复核。“`F:\Projects\` 尚未创建 / G0 未开始”等状态已被同日后续 bootstrap 取代；当前机器状态只以 `docs/governance/governance-index.json` → `current-state.json` → `NEXT/BLOCKED/PROGRESS` 链为准。

## 结论

本轮的**治理文件升级已经闭环**：统一项目根、三分区、项目身份、跨 Harness Skill/记忆、生成物保留与清理、存量迁移边界、开发基线阻断和实际实施顺序已经由同一组权威文件相互引用，未发现仍把旧五分类物理目录当作当前方案的权威结论。

本轮的**治理系统尚未上线**：没有创建 `F:\Projects\`，没有迁移任何项目、工作树、记忆或 Skill，没有实现 Project Home schema、Host validator、三分区创建器、清理调度器或迁移执行器。B1b 继续被基线分叉阻断。

## 复核范围

- 决策：`docs/adr/ADR-009-F盘统一项目根与三分区治理.md`
- 权威合同：`docs/governance/统一项目目录与三分区治理合同.md`
- 联动权威文件：D3、D4、D5、LLM 项目治理说明书、项目路径绑定盘点表
- 活跃状态文件：`docs/INDEX.md`、`docs/PROGRESS.md`、`docs/BLOCKED.md`、`docs/NEXT.md`
- 机器证据：B1a 定向测试、Project Control 全量测试、launch gate、governance gate、桌面快捷方式和 `F:\Projects\` 实际状态

## 闭环检查

| 检查项 | 结果 | 证据与边界 |
|---|---|---|
| 统一项目根 | PASS（文件合同） | 当前权威根为 `F:\Projects\`；尚未创建目录 |
| 项目三分区 | PASS（文件合同） | 每个项目固定 `workspace/worktrees/local`；分类改为元数据，不再建平台/产品/业务物理层 |
| 项目身份 | PASS（文件合同） | 三分区和各 Harness 必须解析同一 `project_id`；现有 DB location 类型不得冒充三分区模型 |
| Skill 共用 | PASS（路线闭环） | D5 的 toolbox 作为规范源，采用受管投影与 hash receipt；尚未实施跨端投影 |
| 记忆共用 | PASS（路线闭环） | D4 的 memory-host 使用单写者、共享数据根和显式 `project_id`；尚未迁移 Stable 数据 |
| 生成物清理 | PASS（合同） | 必须 register → plan → apply → verify → receipt，并支持 PINNED、保留策略、容量门槛和中断恢复；调度器尚未实现 |
| 存量迁移 | PASS（安全边界） | 必须逐项目、可回退、先复制核对再切换；本轮没有搬迁或删除 |
| B 线开发基线 | BLOCKED | 旧 B 为 `6481c47 / 0.4.3`，下一父基线为 A 最终 `c27e989 / 0.4.5`；G0 receipt 前禁止 B1b |
| 桌面入口 | PASS | 用户桌面的 Test、Dev、重复 Stable 共 3 个快捷方式已移入回收站；公共桌面只保留 Stable 0.4.5 |

## 机器复核结果

- `node --test "plugins/project-control/test/approval-card-contract.test.js"`：28 pass，0 fail，0 skipped，0 todo。
- `node --test "plugins/project-control/test/*.test.js"`：177 pass，0 fail，0 skipped，0 todo。
- `node scripts/verify-launch.js`：退出码 0，`launch-gate: tree is launch-ready.`
- `node scripts/check-governance.js`：退出码 1，仍为两项既有失败：cache fixture 命中 credential pattern；origin remote 存在触发 no-remote 规则。未出现第三项。
- `F:\Projects\`：不存在，符合本轮“不创建、不迁移”的边界。

## 尚未关闭的实施项

1. G0：在 `c27e989` 上建立新 B 基线，按 hash 迁移 B1a 与治理包，生成 baseline promotion receipt。
2. G1：实现 Project Home schema、Host validator、三分区 write plan、崩溃恢复和新模板版本；旧模板 1.0.0 保持不可变。
3. G2：实现 local 生成物登记、清理计划、执行、核验、receipt、磁盘门槛和可证明调度。
4. G3：先在 DSH Dev 与 Codex 试点 toolbox Skill 投影，再对 memory-host 做只读双端试点。
5. G4：从低风险项目开始逐个迁移；每次另行取得路径切换和旧副本清理授权。

## 本轮边界回执

- 未修改产品代码、数据库 migration、HTTP、UI、模板 schema 或 Stable。
- 未创建或写入 `F:\Projects\`，未移动项目，未删除项目文件或历史证据。
- 未修改只读上游 `D:\Deepseek Harness`，未 commit、push、发布。
- 三个多余桌面快捷方式进入回收站，可恢复；开始菜单入口未改。
