# P4-2 验收方案（分阶段、带预期输出，2026-08-17）

> **验收状态：已通过（2026-08-17）**。阶段 A/B/D 全部实测通过；期间修复三个问题：① 作业统计漏报（旧条目无作业行）；② 全库噪声召回（无门槛并集）；③ 弱相关噪声（相对剪枝 0.6→0.75）。启动方式为 `启动 DeepSeek Harness 开发版-Hybrid.cmd`。
> 验收对象：P4-2 向量嵌入（回填 + 混合召回）。hybrid 召回默认关闭，阶段 D 需显式开启（真实数据门）。
> 前置：本机模型目录 F:\AI\bge-m3-onnx 已就位（Codex 下载并 SHA-256 校验）；开发版从源码启动（main.js 已配置「检测到模型目录则自动开启嵌入」，重启即生效）。

## 阶段 A · 状态与就绪（约 1 分钟）

1. 重启开发版，新开一个会话。
2. 对 Agent 说：`用 memory_status 把「向量嵌入」块原样报给我`

**预期输出**（逐行核对）：
- `向量嵌入（P4-2）：已开启`
- 模型目录 `F:\AI\bge-m3-onnx`
- `manifest：OK（bge-m3-onnx-int8，1024 维，q8/cls/l2）`
- `generation：` 后跟 16 位十六进制
- `worker：未加载（首次嵌入时懒加载）`（还没记录过新记忆就是这条）
- `hybrid 召回：未启用（功能门通过后开启）`

**不符合时的分支**：整块显示「未开启」→ 确认启动的是开发版且从源码启动；显示「manifest：无效」→ 把整块贴给我。

## 阶段 B · 回填与落库（约 2 分钟）

1. 对 Agent 说：`记住（确认）：验收专用约定——所有导出文件必须使用 UTF-8 编码。`
   （等它回显「已确认写入 active + user_confirmed」。）
2. 等 30–60 秒（后台回填周期 30s），再让它报一次 memory_status 的向量嵌入块。

**预期**：`jobs：pending 0 / ready ≥1 / failed 0 / stale 0`；`worker：ready`。

**不符合时**：jobs 里 failed > 0 → 把整块 + 宿主日志里 `[dsh-memory]` 开头的行贴给我。

## 阶段 C · 降级路径（已由自动化验证，不重复操作）

- 模型目录缺失/损坏 → 状态块显式 `semantic_unavailable`、召回维持 FTS（memory-p4-embedding 测试覆盖；打包冒烟覆盖）。此步跳过人工操作。

## 阶段 D · 真实数据门：hybrid 召回（约 5 分钟）

用 PowerShell 启动开发版（在平时启动命令前加一行环境变量）：

```powershell
$env:DSH_MEMORY_HYBRID = '1'
pnpm start   # 或你平时的启动方式
```

1. 新会话，确认状态块显示 `hybrid 召回：已启用（FTS+向量 RRF 融合）`。
2. 写一条「故意字面不匹配」的记忆：`记住（确认）：出包之前必须把完整测试和发布预检全部跑完，这是发布门禁。`
3. 等 30–60 秒（jobs ready 上涨）。
4. **换说法查询**：`memory_query 发布之前有什么门禁要求`
   - 预期：召回出第 2 步那条（FTS 单靠「发布/门禁」可能漏，Hybrid 应救回——这正是 P4 的价值）。
5. **无关查询**：`memory_query 今天晚饭吃什么`
   - 预期：不返回任何记忆正文（无需历史的任务）。
6. **跨项目串味（可选）**：打开项目控制台绑定一个项目，再查一个只有该项目有的词 → 结果只含该项目记忆，不混入全局/其他项目。

**不符合时**：把第 4 步的完整查询输出贴给我。

## 阶段 E · 性能体感（顺手观察，不设硬指标）

- 首次触发嵌入后，任务管理器里 DSH 进程内存增加约 0.9GB（模型驻留，属预期）。
- 查询/写入无体感卡顿（单句嵌入 ~15ms 量级）。
- 完全离线：断网状态下重复阶段 B/D 应一切正常（嵌入不走网络）。

## 验收结论判定

| 结果 | 判定 |
|---|---|
| A+B 通过，D 第 4 步救回成功 | **P4-2 通过**，真实数据门初判通过 |
| D 第 4 步救回失败 | 贴输出给我，我调融合参数/阈值后重测 |
| jobs failed 持续 >0 | 贴日志，我修管线 |
