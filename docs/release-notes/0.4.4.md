# DeepSeek Harness Personal v0.4.4（稳定版·生产缺陷修复候选）

## 概述

本版修复 v0.4.3 真实 Stable 重启后外部插件 `pending generation` 无法激活的生产缺陷。

- 修复真实 Stable pending 插件 generation 无法激活：主进程现在把由实际 `userData` 派生的 `plugins-external` 绝对路径可信传递给 Harness helper，不再依赖用户手工环境变量；Stable 缺路径时 fail-closed。
- 全量测试：**731/731**，fail 0，skipped 0。
- 真实 packed Stable 三启动 E2E 通过：pending → activating → current、再次冷启动 ACTIVE、回滚后冷启动回到内置。
- 0.4.3 为已知良好包，可用于客户端自动回滚。
- 当前真实 Stable 已有的 pending generation 会在升级到 0.4.4 后尝试自然激活。

## 发布状态

- 本文件为发布资料收口，仍不执行 commit/push/release，不安装真实 Stable。
