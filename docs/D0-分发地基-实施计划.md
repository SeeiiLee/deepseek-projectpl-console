# D0 分发地基实施计划（架构书 v5 §15/§23 落地）

> 日期：2026-08-18
> 依据：docs/工作台-项目控制台-联动架构设计书.md v5（已接受，commit ec02bbd）
> 目标：先证明「包离开当前 checkout 仍能安装和组合」，再进入 W0（Workspace Hub）与 R-PV1（Markdown 排版）双线。
> 红线：不改只读上游 D:\Deepseek Harness；不装依赖（CodeMirror 等留 R-ED 单独授权）；不发布、不迁移真实数据；只用临时 DSH_HOME 与本地 .tgz 做 PoC。

---

## 1. 已核实事实基线（2026-08-18 实测，证据路径见列）

| 事实 | 实测结果 | 证据路径 |
|---|---|---|
| 活动运行时 | rc.7（0.1.0-rc.7），commit 99f6f02fecdb7dff40c3fbc9470f5907c29f74ca，activeHarnessRoot 已激活 | update-center.json（Dev AppData）|
| 回滚位 | rc.5（0.1.0-rc.5），commit 47f943859bef60e4160492346772ded9b24f765a，只读源码树，无漂移（仅 1 个未跟踪启动脚本）| D:\Deepseek Harness git HEAD + status |
| 官方安装 seam | `dsh plugin --profile <name> <pnpm args>` 存在：初始化 profile → 转发 pnpm → 按已安装状态 reconcile `dsh.profile.bundles`（声明 `dsh.bundle.patch` 的依赖加入 layer，移除/失去声明则离开）；相对路径参数锚定调用目录 | rc.7 apps/cli/src/plugin.ts |
| Profile 合同 | `$DSH_HOME/profiles/<name>/package.json`（dependencies + dsh.profile.bundles）+ 用户 patch 层 cordis.patch.yml；bundle layer 顺序应用后接用户 patch；模板 web = dsh-base + dsh-web-app | rc.7 packages/boot/app-boot/src/profile.ts |
| ui-primitives | `@deepseek-ai/dsh-client-ui-primitives` 在 PLATFORM_MODULES；导出 MarkdownText、CodeBlock、MarkdownFileMentions（type，fileMentions prop）；KaTeX/Shiki 内置于上游实现 | rc.7 packages/client/web/src/platform.ts、packages/client/ui-primitives/src/index.ts |
| Workbench bundle 基线 | lib/client.js = 130,032 B（≈127 KiB）| plugins/workbench/lib/client.js |
| 当前插件形态 | 17 个目录，17/17 private:true，0/17 声明 dsh.bundle.patch；激活 = 桌面 overlay（plugins/cordis.patch.yml）+ junction 链接进 ~/.dsh/profiles/web/node_modules | src/personal-plugins.js、plugins/cordis.patch.yml、~/.dsh/profiles/web/package.json |
| 构建系统现状 | scripts/build-plugins.js 默认 DSH_SOURCE_ROOT=D:\Deepseek Harness（rc.5）调 tsdown；17 个插件 tsconfig/tsdown 硬编码 `../../../Deepseek Harness/...` 相对引用（rc.5 合同）| scripts/build-plugins.js、plugins/*/tsdown.config.ts |
| 依赖归属 | pnpm-workspace.yaml 无 packages: 段，plugins/* 不是 pnpm workspace 包；根 lockfile 无 @cyrus 条目 → 插件依赖仅靠根 node_modules 偶然可解析（架构书 C-25 问题成立）| pnpm-workspace.yaml、pnpm-lock.yaml |
| 开发版 profile | ~/.dsh/profiles/web：bundles=[dsh-base, dsh-web-app]，dependencies 空，个人插件不在其中（非 bundle 形态）| ~/.dsh/profiles/web/package.json |

## 2. D0 步骤与门禁

### D0.1 合同冻结（本阶段已完成的部分）
- [x] dshComposable/v1 Schema（package.json 扩展字段，仅 installer/doctor 读取）
- [x] preset-lock（plugin-set.lock.json）Schema
- [x] 一致性检查器 scripts/check-bundle-identity.js（name = clientBundle(id) = patch row name；dsh.bundle 在位）
- [x] 基线证据入库：检查器跑 17 插件 → 0/17 有 dsh.bundle、17/17 handoff id 正确、17/17 row id 为短名（与架构书 §0.7 表格一致）；test/check-bundle-identity.test.js 7/7 绿

### D0.2 基线决策（**人工审核点 1**，见 §4）
- [ ] 基线策略：rc.7-only（v5 推荐）vs 双基线 rc.5+rc.7
- [ ] 发布形态：仅 .tgz 本地安装 vs npm registry（@cyrus scope 是否注册发布）

### D0.3 build-kit
- [ ] scripts/build-plugins.js 改为显式 DSH_SOURCE_ROOT 必填/校验 rc.7 commit（99f6f02f 前缀校验），不再默认相邻目录
- [ ] 插件 tsconfig/tsdown 迁根到 rc.7 合同（机械修改 17 包，含 peerDependencies 版本）
- [ ] 干净 clone typecheck/bundle 复现验证

### D0.4 PoC A：AnySearch 叶子（完成，证据见下）
- [x] anysearch 补标准 bundle 形态：dsh.bundle.patch=./cordis.patch.yml（row id/name = 包全名 + apiKeyEnv 配置）、LICENSE(MIT)、README 脱敏、files 白名单（7 文件）、去 private
- [x] npm pack --dry-run：7 文件（lib×3 + patch + LICENSE + README + package.json），sourcemap 全相对路径，无个人盘符；SHA-256=D231FFAF00CC37E60DB90905C572B368B3AD70710DC018244F612DD1C4CF8358
- [x] 临时 DSH_HOME + 官方 CLI add <tgz>（pnpm shim 前置）→ 依赖入 profile、reconcile 自动把 @cyrus/dsh-anysearch 加进 dsh.profile.bundles
- [x] 冷启动（runProfile 无 overlay）：readiness 行正常、优雅退出；loader.entries() 显示 include:@cyrus/dsh-anysearch fiber=ACTIVE（searchProviderId=deepseek-official 属预期——provider 选择是 suite 配置层职责）
- [x] remove → bundles 自动回退 [dsh-base, dsh-web-app]、依赖与 node_modules 清除；PoC home 全文零 Personal checkout 引用（删 checkout 不影响）

### D0.5 PoC B：Shell + Workbench 双 row（完成，证据见下）
- [x] personal-shell（adapter）、workbench（core + requires.packages=[@cyrus/dsh-personal-shell]）各自 bundle patch（row id/name = 包全名，单 row 单 owner）+ LICENSE/README/files 白名单
- [x] 干净 profile 双 tgz 直接依赖安装 → 两包入 dsh.profile.bundles；冷启动 loader.entries() 双 fiber=ACTIVE
- [x] 新增 scripts/plugin-installer.mjs（dshComposable 感知 installer/doctor）：graph/add-check/remove-check；remove-check shell → 拒绝（依赖者 workbench 在位）；remove-check workbench → 放行；test 7/7
- [x] 官方 CLI 裸 remove 不拦截（pnpm forwarder 性质），依赖闭包由本层门禁承担——与架构书 ADR-09 B 一致

### D0.6 plugin-set.lock.json（生成器完成；共享 patch 降级待下轮）
- [x] scripts/generate-plugin-set.mjs：collectPlugins/buildInstallOrder（依赖先行+环报错）/packPlugin（npm pack+sha256）/computeLock/locksEquivalent；--check 幂等校验；test 4/4
- [x] 已生成 plugin-set.lock.json（17 包，无个人路径，compatibleHarness=rc.7/99f6f02f；workbench 带 requires）→ schema 校验通过、--check 一致
- [ ] 共享 plugins/cordis.patch.yml 降为生成产物 + PERSONAL_PLUGINS 退役（改动启动链，需完整 smoke 验证，下轮做）

### D0.7 验收证据
- [ ] §17.5 可分发性验收 13 项逐项记录（pack 清单 / 临时 profile / dump-config / 冷启动 / 功能 smoke / 拔 Bridge / 拒绝移除被依赖 Core / 数据保留 / 重装读原数据 / 删 tarball+checkout 重启 / Stock DSH 支持矩阵 / 无个人路径）
- [ ] DEVLOG 更新 + 本计划文档勾选

## 3. 与后续阶段的关系

```text
D0 分发门禁 → Workspace 主线：W0（Task A/B）→ W1（Task C/D/E）→ W2…
D0 分发门禁 → 文档体验线：R-PV1（Task F）→ Document Session → R-ED（Task G，CodeMirror 需单独授权）
```

并行边界（架构书 C-20）：A/B/D 不改 plugins/workbench；F/G 不改 plugins/workspace-hub 与 Project Control migration；WorkspacePreviewViewer.tsx / Workbench manifest / Tab-Document 接线由单一集成人修改。

## 4. 人工审核点清单

| # | 时点 | 需要 Cyrus 拍板的内容 | 我给出的建议 |
|---|---|---|---|
| 1 | D0.2（进行中） | 基线策略：rc.7-only 还是双基线 | rc.7-only（v5 推荐）：17 包 peer/构建合同迁到 rc.7；回滚 rc.5 时 Personal 插件降级停用 |
| 1b | D0.2 | 发布形态：仅 .tgz（本地/内网）还是上 npm registry | 先仅 .tgz：@cyrus 保持私有，PoC 用本地 tarball；registry 发布可在首个稳定版前再决定 |
| 2 | D0.4 | PoC A 包装 anysearch 为正式 bundle（改其 package.json/tsdown，行为不变）| 同意即执行；dev 版运行不受影响（overlay 激活不变）|
| 3 | D0.5 | shell/workbench 拆独立 row 后，共享 patch 的桌面 overlay 语义核对 | 由我生成 diff 供核对 |
| 4 | R-ED 前 | CodeMirror 6 六包安装授权（根 lockfile 锁定 + 许可审计）| 到时再单独请示，含费用/体积/许可证清单 |
| 5 | 发布前 | 首个可发布版本范围与发布渠道 | 到时按 PUBLISHING_RULES 走 preflight |

## 5. 本次会话已交付

- 本计划文档
- protocol/personal-plugin-contract/v1/schemas/（dsh-composable + preset-lock，各含 valid/invalid 示例，Ajv strict 校验通过）
- scripts/check-bundle-identity.js + test（7/7）：基线 0/17 bundle → 现 anysearch/shell/workbench 3 包 ready（bundle + handoff + row 全名一致）
- scripts/build-kit.mjs + test（7/7）：rc.7 构建根解析（DSH_SOURCE_ROOT 显式 / 活动托管运行时自动）+ commit 校验 fail closed + harness-src junction；build-plugins/check-plugins/smoke 全部接入
- **基线迁移 rc.5 → rc.7-only（Cyrus 拍板）**：17 包 tsconfig/tsdown 迁 harness-src、peer/version 迁 0.1.0-rc.7；17/17 typecheck 绿；全部 bundle 重建；verify-launch/governance（refs 跳过修正）/全量 487/487/dev Electron smoke 全绿
- **PoC A（anysearch）**：标准 bundle 化（README 脱敏、MIT LICENSE、7 文件 pack）→ 干净 profile 官方 CLI 安装/reconcile/冷启动 ACTIVE/移除无残留/零 checkout 引用
- **PoC B（shell+workbench）**：双 bundle + 依赖闭包门禁（plugin-installer.mjs，7/7）；remove-check 拒绝先移除 shell；冷启动双 ACTIVE
- **D0.6 生成器**：plugin-set.lock.json（17 包，schema 校验通过）
- 基线决策已拍板：rc.7-only + 仅本地 .tgz（审核点 1/1b/2 全部通过）
