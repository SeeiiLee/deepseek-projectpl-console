# 项目路径绑定盘点表（迁移第 0 步）

> 状态：v2 权威实测盘点 ｜ 初次盘点：2026-08-21 ｜ 2026-08-25 按 ADR-009 更新目标目录合同 ｜ 作者：Kimi K3 / 联动修订：Codex
> 数据来源（全部本轮实测）：DSH 开发库/稳定库 project-control SQLite（projects + project_source_roots + project_path_history 三表直读）；Codex `config.toml` 项目信任列表 + 308 会话 by_cwd 全量分布；Kimi/Kimi Work/QClaw 目录枚举
> 用途：项目统一迁移（软链过渡法）的绑定清单；每行 = 一个真实项目资产 + 它被哪些端绑定、绑在哪

---

## 一、主项目资产（4 个，必须迁移妥善）

### 1. DSH Desktop（本项目）

- **canonical Project Home**：`F:\Projects\deepseek-harness-personal`；旧 `D:\Deepseek Harness Personal` 只读保留为历史迁移输入
- **DSH 开发版**：测试登记 `prj_01a00cfd-1fe4-7bb3-9123-027765662055`（single_project 源根），只作开发回归输入，不参与正式项目身份
- **DSH 稳定版**：正式项目 `prj_01a0082e-fea8-7d6f-b6c2-08a259fba389`
- **Kimi Work**：当前会话所在项目工作区（本盘点即在此上下文产出）
- **Codex**：42 个会话（507 MB，第二大语料源）
- **当前状态**：G0.5 canonical Git 基线已提交为 `28d7c8c`；G1 Project Home 产品合同已本地实现。Dev ID 已明确降为测试身份，不再构成正式身份冲突；真实 Stable binding 仍指向旧目录，须另案迁移，旧目录不清理。

### 2. meal_tracker（食溯 NutriSight）

- **物理路径**：`F:\QClawData\workspace\meal_tracker`
- **DSH 开发版**：已注册 `prj_01a00719-...`（source_root 类型）
- **DSH 稳定版**：已注册 `prj_01a0109b-...`
- **Codex**：信任条目在册；**192 个会话（2271 MB，最大语料源）** + 6 个 worktree 临时 cwd（`C:\Users\Administrator\.codex\worktrees\*\meal_tracker`，C 线 alias 表已覆盖）
- **QClaw/OpenClaw**：宿主目录本身（`F:\QClawData\workspace\` 是 OpenClaw 的工作区根，含 AGENTS.md/MEMORY.md/skills 等）
- **迁移要点**：**项目与 OpenClaw 工作区根是父子关系**——要么只搬 meal_tracker 子目录（OpenClaw 根不动），要么连 OpenClaw 工作区整体规划；建议前者，影响面最小

### 3. Amazon Store（亚马逊）

- **当前文件系统 Project Home**：`F:\Projects\amazon-store`；`workspace` 已完成 5,252 个源文件复制并补 4 份治理入口，3 个加密文件只在 `local/secure`，新 runtime 为空
- **旧物理路径**：`F:\documents\Kimi\Workspaces\Amazon Store`；仍完整保留作回滚和旧 `Temp/tmp` 历史证据，不再作为新工作的默认写入点
- **DSH 稳定版正式身份**：2026-08-25 经 Cyrus 确认并对受保护 Stable SQLite 做只读、query-only 复核，唯一项目为 `prj_01a01cb7-b3f5-7dd3-932f-1adc4d16a1dd`，名称「亚马逊运营主架构 · PRD」，active primary location 为 `F:\documents\Kimi\Workspaces\Amazon Store`
- **DSH 开发版**：以 source_root 挂在 tools 项目下（`prj_01a00784-...`），仅作测试登记，不参与正式身份或 binding 决策
- **Codex**：信任条目在册；8 个会话（106 MB）
- **Kimi Work**：现有 workspace ID `wd_amazon-store_7f6c65590a90` 与历史会话均绑定旧路径；正式接口不支持修改既有 workspace root
- **迁移状态**：文件系统试迁和正式身份对齐已通过；canonical workspace 为 `F:\Projects\amazon-store\workspace`。下一步先由 Codex 使用 canonical 路径，再以可回滚 junction 保留 Kimi 旧入口；Kimi 验收通过后才更新 Stable binding，不能以手工改 Kimi 内部 JSON 代替

### 4. Cyrus Quant Trading（量化）

- **物理路径**：`F:\documents\Kimi\Workspaces\Cyrus Quant Trading`——**目录名含不可见字符 ZWNJ（U+200C）**，开发库/稳定库的 normalized_path 都带它
- **DSH**：开发库 `prj_01a00c8d-...` + 稳定库 `prj_01a01cb8-...` 均已注册
- **Codex**：信任条目在册；12 个会话（30 MB）
- **迁移要点**：**迁移是去除 ZWNJ 的最佳时机**——新路径用干净目录名，junction 从旧（含 ZWNJ）路径指过去，历史会话 cwd 经 C 线 alias 表映射；各端重新绑定时全部用干净路径

## 二、次要/实验性资产（建议归档而非迁移）

| 路径 | 会话量 | 处置建议 |
|---|---|---|
| `D:\CodexData\workspaces\Codex Pets` | 12 个（215 MB） | 评估后归档 |
| `F:\documents\Kimi\Workspaces\Cyrus Music` | 1 个 | 归档 |
| `D:\CodexData\workspaces\Codex Dream Skin` | 1 个 | 归档 |
| `F:\documents\Kimi\Workspaces\Amazon` | — | **已查明非项目（2026-08-21）**：纯 Electron Crashpad 崩溃残留（3 文件 1.1 MB，reports/attachments 均空，无用户数据），建议直接删除，不进迁移清单 |
| `F:\documents\Kimi\Workspaces\Normal Daliy Chat` | — | 非项目，不迁移 |
| `C:\Users\Administrator\Documents\Codex\2026-*`（new-chat、n2-* 等十余个一次性目录） | ~20 个 | 一次性会话沙箱，不迁移 |
| `C:\Users\Administrator\.codex\worktrees\*` | 6 个 | Codex 临时 worktree，自动管理，不动 |

## 三、各 Harness 数据/配置锚点（迁移时不动，但要知道在哪）

| 端 | 数据/配置位置 | 绑定机制 |
|---|---|---|
| DSH 开发版 | `%APPDATA%\DeepSeek Harness Personal Dev\` | project-control 注册表（有 project_path_history / rebind_proposals 表，上游预留了换路径机制） |
| DSH 稳定版 | `F:\documents\Cyrus Deepseek Harness Data\` | 同上 |
| Codex | 配置 `%USERPROFILE%\.codex\config.toml`；会话 `D:\CodexData\home\sessions` | 项目信任列表按路径小写键入；历史会话 cwd 已由 C 线 alias 表兜底 |
| Kimi Work | `D:\KimiWork\`（daimon-share / kimi / workspace） | 项目目录即工作区 |
| Kimi 桌面版 | `F:\documents\Kimi\Workspaces\` | 目录成员制 |
| QClaw/OpenClaw | `F:\QClawData\workspace\` | 工作区根 |

## 四、迁移预备结论（2026-08-25 以 ADR-009 为准）

1. **新根已拍板（2026-08-21）**：`F:\Projects\`（独立于任何 Harness 数据目录）
2. **Project Home 三分区已拍板（2026-08-25）**：物理第一层直接使用稳定项目 slug；项目分类和生命周期全部进入 Project Control 元数据，不再使用 `platform/products/business/lab/archive` 物理分类层。

```
F:\Projects\
├── deepseek-harness-personal\
│   ├── workspace\
│   ├── worktrees\
│   └── local\
├── meal-tracker\{workspace,worktrees,local}\
├── amazon-store\{workspace,worktrees,local}\
├── cyrus-quant-trading\{workspace,worktrees,local}\
├── toolbox\{workspace,worktrees,local}\
└── memory-system\{workspace,worktrees,local}\
```

- `workspace` 是 canonical 项目根；`worktrees` 放开发 checkout；`local` 放运行时/构建/E2E/cache/receipt。详细合同见 `docs/governance/统一项目目录与三分区治理合同.md`。
- D3 十二目录只落在每个项目的 `workspace/docs`，与外层三分区互不替代。

3. **G4 迁移顺序（Cyrus 2026-08-25 最终拍板）**：Amazon Store 主要是 skills 与调研报告，先迁（→ `amazon-store\workspace`）→ Quant 只有一套初版架构，第二个迁并去除 ZWNJ（→ `cyrus-quant-trading\workspace`）→ meal_tracker/食溯是复杂项目，最后迁（→ `meal-tracker\workspace`）。DSH Desktop 的文件基线已先完成 canonical 收敛，不混入这三个项目的 G4；其真实 binding/双 ID 另案处理。
4. **软链过渡**：每个项目通过迁移 plan 后，旧路径可在获批观察期内 junction 到新 `workspace`；各端分批改 binding，全部改完且 retire receipt 通过后再删 junction。Project Home 根本身不作为旧项目 cwd 的替代目标。
5. **待决项**：~~①`Amazon` vs `Amazon Store`~~（已查明为 Crashpad 残留，已于 2026-08-21 经 Cyrus 确认删除）；②meal_tracker 与 OpenClaw 工作区根是否联动迁移（建议只搬子目录）；③开发库 tools 项目下 Amazon source_root 迁移后是否清理
