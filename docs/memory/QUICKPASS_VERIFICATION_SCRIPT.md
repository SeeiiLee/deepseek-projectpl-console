# QUICKPASS_VERIFICATION_SCRIPT.md（quick-pass 真实会话验证脚本）

> 用途：P2 收尾的真人验收脚本（Cyrus 执行）。前置：开发版客户端已换装「P2 收尾」新包
> （记忆库落在开发版数据目录、quick-pass 已开启、加密引擎已接入）。
> 判定标准写在每段末尾；全部通过后 quick-pass 才按项目正式启用（稳定版默认仍关闭）。

## 第 0 段 · 装包自检（必做，防旧包）

1. 托盘退出正在运行的旧测试版；
2. 直接双击：分发包\测试版\DeepSeek-Harness-Personal-Dev-0.1.1-portable-x64.exe（不要用桌面旧图标，它指向旧版）；
3. 先问一句：「你现在有哪些记忆工具？」——应能答出 memory_record / memory_summary / memory_query 等；
4. 再问：「你说说我们的红线有几条？」——能答出 8 条红线即新包生效。

> 第三轮（2026-08-15/16，最终结论：**quick-pass 验证通过**）：
> ① 亚马逊店铺 → 自动归类 global_user/global_fact ✓；② 食溯进度/GBK 坑 → 识别为项目专属，
> 拒绝擅自落全局，用提问卡征求用户同意后才暂存 global pattern 并注明迁移计划 ✓（分类合同完全生效）；
> ③ 显式召回/新会话召回/翻译无干扰 均已通过。剩余：项目登记完成后把暂存条目迁回项目级。
>
> 第二轮进度（2026-08-15，新包 0.1.1）：自检通过（9 个记忆工具描述准确）、红线自检通过（8 条）；
> 播种 1/3：memory_record 写入 project_fact 成功，条目 01a006b6-…（已只读核验落盘，ID 与报告一致）。
> 亮点：scope=project 因「食溯」未在开发版 Project Control 登记而被 fail-closed 拒绝，agent 透明说明并询问用户
> （合同行为，非 bug）；条目暂落 global_user，稳定版中食溯已登记，届时可走项目 scope。
>
> 第一轮失败记录（2026-08-15/16）：当时运行的是旧 0.1.0 包（无记忆工具、无红线插件），
> agent 声称「写了 PROJECT_CONTEXT.md」与「批准策略切换为 never」——只读排查确认两个声明均未落盘，
> 属模型幻觉（批准设置未变、文件不存在）；已加工具路由指引与 AGENTS 路由规则修复。

## 第 0.5 段 · 归类规则（2026-08-15 增补，播种前先看）

- 新硬规则：项目专属内容（项目定位、检测报告模板）必须 scope=project（未登记项目会被拒绝并提示）；
  跨项目通用教训（如「导出统一 UTF-8」）用 scope=global_user + kind=pattern；拿不准先 memory_classify。
- 旧三条测试数据在开发库（2 条 project_fact 落在全局），可用 memory_archive 归档后按新规则重记。

## 第 1 段 · 播种（3 条「记住」）

- 「记住：我们公司做食品溯源 SaaS，项目代号食溯」
- 「记住：检测报告模板用 v2 版，导出编码必须是 UTF-8」
- 「记住：上次发布后修过导出乱码的坑，根因是 GBK 代码页」

注意：内容里不要放密码/身份证/卡号——写入门禁会硬拒绝（预期行为，不算 bug）。
若想更快，可直接对 agent 说「用 memory_record 记这三条，confirm=true」。

## 第 2 段 · 显式召回

问：「我们之前定的检测报告模板是哪个版本？」
预期：答出「v2 版」，并带「历史记忆 / 可能过时」的引用语气。

## 第 3 段 · quick-pass 自动注入（关键：全新会话，最好重启一次客户端）

什么都不交代，直接问：「上次说的导出乱码那个坑，根因是什么来着？」
- 通过：新会话零背景却答出「GBK 代码页」。
- 失败：答「不知道」→ 把现象反馈给 DeepSeek。

## 第 4 段 · 无干扰性

说：「翻译一下 hello」。
预期：正常翻译，**不出现任何记忆内容**。

## 第 5 段 · 可选：跨项目不串味

若开发版里登记了多个项目（食溯 / 商城），分别问各自的问题，确认不张冠李戴。

## 第 6 段 · 反馈（三句话）

1. 新会话能不能答对？
2. 无关对话有没有被记忆打扰？
3. 有没有报错、变慢或异常？

---

> 验收记录：完成后把结果回写本文件（日期 + 三句反馈 + 通过/失败），并通知 DeepSeek。
