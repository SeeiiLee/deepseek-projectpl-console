# R-ED Phase 1 审核清单（Cyrus）

> 用途：Cyrus 实测开发版后逐项确认；确认通过后进入 Phase 2。
> 关联：`docs/RICH_TEXT_EDITOR_REFACTOR.md`、`docs/NEXT.md`。

## 1. 基本编辑体验

- [ ] 打开 `M9_DESIGN.md`，点「编辑」后进入富文本编辑，不再看到 CodeMirror 行号/代码样式。
- [ ] 标题、段落、列表、引用、代码块以“预览同款”的块级样式显示。
- [ ] 选中文本不再错位，拖动选择范围正常。
- [ ] 不再出现多余符号：`#`、`>`、`*`、`1.1` 等。
- [ ] 引用块行首文字没有被吞字（如“日”字）。
- [ ] 长文档可以滚动到底，不会只显示一页。

## 2. 格式工具栏与命令

- [ ] 静态工具栏存在：H1–H6、加粗、斜体、删除线、行内代码、无序/有序列表、引用、代码块。
- [ ] 点击加粗/斜体等，选中文字格式生效。
- [ ] 切换标题层级生效。
- [ ] 输入 `/` 能弹出斜杠命令菜单。
- [ ] `/h1`–`/h6`、`/ul`、`/ol`、`/todo`、`/quote`、`/code`、`/hr` 能创建对应块。
- [ ] 输入 `# `、`## `、`- `、`1. `、`> `、` ``` `、`---` 能触发 Markdown 快捷键。
- [ ] 输入 `#1`、`##2`、`###3`、`-1` 这类无空格形式也能转换。

## 3. 阅读设置联动

- [ ] 修改字号，编辑区正文字号跟随变化。
- [ ] 修改字体，编辑区正文字体跟随变化。
- [ ] 修改文字颜色，编辑区正文颜色跟随变化。
- [ ] 修改阅读宽度，编辑区内容宽度跟随变化。
- [ ] 代码块使用代码字体设置。

## 4. 保存与源文件

- [ ] 编辑后保存，`.md` 文件内容语义不变。
- [ ] 项目视图内保存不再提示“项目文件只读”。
- [ ] 接受格式规范化（列表符号、空行、加粗符号等可能被统一）。
- [ ] 保存后重新打开，内容能正确加载回富文本。
- [ ] 版本 `sha256` 与字节数显示在正文右下角（随文档滚动，不是独立底栏）。

## 5. 原有能力

- [ ] 自动保存（2s debounce）仍然工作。
- [ ] Ctrl+S 保存仍然工作。
- [ ] 放弃更改（X）仍然工作。
- [ ] 外部修改冲突三态仍然工作。

## 6. 回归门禁

- [ ] 单测：`586/586`
- [ ] Workbench 测试：`103/103`
- [ ] Smoke：`SMOKE_EXIT=0`

## 结论

- [ ] Phase 1 通过，可以进入 Phase 2。
- [ ] 需要先修复以下问题：
