//#region src/index.ts
/** Host loader entry for the personal connection-center client plugin. */
/** This package intentionally has no Host-side behavior. */
function apply() {}
//#endregion
export { apply };
