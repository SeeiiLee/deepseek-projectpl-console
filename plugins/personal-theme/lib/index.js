//#region src/index.ts
/** Host loader entry. Persistence and HTTP transport are owned by personal-foundation. */
/** The personal theme has no Host behavior of its own. */
function apply() {}
//#endregion
export { apply };
