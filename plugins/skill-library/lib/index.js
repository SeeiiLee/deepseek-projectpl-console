//#region src/index.ts
/** Host loader entry for the personal Skill library client plugin. */
/** This package intentionally has no Host-side behavior. */
function apply() {}
//#endregion
export { apply };
